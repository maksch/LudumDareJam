using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TeleportLimits : MonoBehaviour {
 //   float currentX;
 //   float currentY;
 //   float lastX;
 //   float lastY;

	//// Use this for initialization
	//void Start () {
 //   }
	
	//// Update is called once per frame
	//void Update () { 
 //       currentX = transform.position.x;
 //       currentY = transform.position.y;

 //       if (Mathf.Sqrt(Mathf.Abs(currentX * currentX)  + Mathf.Abs(currentY * currentY)) > 9)
 //       {
 //           if(lastY > currentY || lastY < currentY)
 //           {
 //               transform.position = new Vector3(transform.position.x, -transform.position.y * 0.99f, transform.position.z);
 //           }
 //           if (lastX > currentX || lastX < currentX)
 //           {
 //               transform.position = new Vector3(-transform.position.x * 0.99f, transform.position.y, transform.position.z);
 //           }
 //       }

 //       lastX = currentX;
 //       lastY = currentY;
	//}
}
